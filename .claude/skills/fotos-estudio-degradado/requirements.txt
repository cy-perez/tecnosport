# Dependencias de fotos-estudio-degradado (Python 3.11 o posterior; probado con 3.12).
# Instálalas con el MISMO Python que ejecutará los scripts:
#   python -m pip install -r requirements.txt
# rembg ya no instala OpenCV por su cuenta: por eso va explícito.
rembg[cpu]>=2.0.60,<3
opencv-python-headless>=4.8,<5
numpy>=2.0
pillow>=12.0
pillow-heif>=1.0
pymatting>=1.1.14
